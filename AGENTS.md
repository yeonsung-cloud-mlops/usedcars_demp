# 작업 지침

이 지침은 저장소 전체에 적용합니다.

## 프로젝트 이해

- 과거 중고차 매물 가격을 추정하는 scikit-learn 모델과 FastAPI·Next.js 데모입니다.
- 사용자 설명과 문서는 기본적으로 한국어로 작성하고 코드 식별자는 기존 영어 표기를 따릅니다.
- 현재 동작은 소스 코드와 모델 메타데이터를 기준으로 확인합니다. 과거 비교 문서나 설계 문서를 현재 구현으로 간주하지 않습니다.
- 루트 `README.md`에 설치, 모델 준비 조건, 실행 및 검증 절차가 있습니다.

## 모델 및 데이터 계약

- 원본 CSV는 수정하지 않습니다. 전처리·학습 결과는 `artifacts/`에 저장합니다.
- 차령은 등록 연도에서 차량 연식을 뺀 정수입니다. 현재 연도를 과거 데이터에 일괄 적용하지 않습니다.
- 가격·주행거리 단위를 임의로 통화, km 또는 mile로 해석하거나 변환하지 않습니다.
- 등록일 기준 분리와 학습·평가 VIN 비중복을 유지하며 평가 정답이 학습이나 인코딩에 유입되지 않게 합니다.
- 사고 이력의 미상은 무사고와 구분합니다. 미학습 차종 거부 및 미학습 트림의 이전 모델 대체 예측을 보존합니다.
- Feature 변경 시 전처리, 학습, Python 예측, CLI, API, UI 및 테스트의 영향을 함께 확인합니다.
- 모델 번들과 pickle 파일은 신뢰할 수 있는 로컬 산출물만 로드합니다.
- 재학습은 기존 산출물을 덮어쓰므로 요청 범위와 실행 필요성을 확인하고 필요한 이전 모델을 보존합니다. 문서 작업만으로 대용량 전처리나 재학습을 실행하지 않습니다.

## 구현 및 검증

- 변경 목적에 필요한 범위만 수정하고 관련 없는 사용자 변경은 되돌리지 않습니다.
- Python 3.11 및 고정된 requirements를 사용합니다. 프런트엔드는 lockfile 기준으로 `npm ci`를 사용합니다.
- 모델·API 변경 후 모델 파일이 준비된 환경에서 다음을 실행합니다.

  ```bash
  OMP_NUM_THREADS=4 OPENBLAS_NUM_THREADS=4 python -m pytest -q test_price_model.py backend/tests/test_api.py
  ```

- 프런트엔드 변경 후 `frontend/`에서 `npm run typecheck`와 `npm run build`를 실행합니다.
- 모델 파일이나 의존성이 없어서 검증할 수 없다면 필요한 파일과 실행하지 못한 검증을 명시합니다. 결과를 추측해 통과로 기록하지 않습니다.
- API의 입력 검증, 오류 응답, 요청 ID, 준비 상태 확인 및 동시 예측 제어를 유지합니다.
- API가 트림을 받는다고 가정하지 않습니다. 현재 트림 입력은 Python·CLI에서만 제공합니다.
- 현재 시세, 실거래가, 보장된 오차 범위라는 표현을 사용하지 않습니다.

## Git 및 배포

- 실습 공유용 `used_cars_price_prediction.csv`는 Git LFS로 추적합니다. 이 파일 외의 CSV, `artifacts/`, `.env` 및 인증 키, `deploy/.local/`, 의존성·빌드 캐시는 커밋하지 않습니다.
- 커밋 전 `git diff --check`와 스테이징 파일 목록을 확인합니다.
- AWS 스크립트는 실제 리소스를 변경합니다. 배포 작업 요청 범위에서만 실행합니다.
- 모델 및 실행 조건이 바뀌면 README도 함께 수정합니다.
